---
slug: transitive-closure
order: 5
group: connectivity
title: "סגור טרנזיטיבי"
summary: "גרף הנגישות G*: קשת (u,v) קיימת אם ורק אם יש מסלול מכוון u→v ב-G."
frequency: "8/19"
difficulty: medium
complexity:
  time: 'O(|V|^3)'
  timeNote: "Floyd-Warshall בוליאני; או O(|V|·(|V|+|E|)) ע\"י BFS/DFS מכל צומת"
  space: 'O(|V|^2)'
  spaceNote: "מטריצת הנגישות בגודל V × V"
examFrequency: "נושא 5 · סגור טרנזיטיבי — ~8/19 מבחנים (לרוב רב-ברירה 3ה)"
---

**המטרה:** לבנות את ה**סגור הטרנזיטיבי** `G* = (V, E*)`, כאשר `(u, v) ∈ E*` **אם ורק אם** קיים מסלול מכוון `u → v` ב-`G` (כלומר `v` **נגיש** מ-`u`).

- `G*` הוא בדיוק יחס ה**נגישות** — הוא עונה בבת-אחת על "האם ניתן להגיע מ-`u` ל-`v`?" לכל זוג.
- **כל קשת של `G` נשמרת**: תמיד `E ⊆ E*` (קשת בודדת היא מסלול באורך 1). אך `|E| < |E*|` **אינו** תמיד חד-משמעי — אם `G` כבר טרנזיטיבי, אז `E = E*`.
- הסגור של **DAG נשאר חסר-מעגלים**: מעגל ב-`G*` היה מחייב מעגל ב-`G`.

## איך זה עובד

**שיטה 1 — Floyd-Warshall בוליאני (`O(|V|³)`).** קח את נוסחת הנסיגה של [[Floyd–Warshall Algorithm|פלויד-וורשל]] והחלף את `min/+` באופרטורים הבוליאניים `OR/AND`:

1. אתחל `t[i][j] = true` אם `(i, j) ∈ E` או `i = j`, אחרת `false`.
2. לכל צומת ביניים `k = 1..n`, לכל זוג `i, j`: עדכן `t[i][j] = t[i][j] OR (t[i][k] AND t[k][j])`.
3. לאחר ה-`k` האחרון, `t[i][j]` אומר אם `j` נגיש מ-`i`.

**שיטה 2 — `|V|` סריקות (`O(|V|·(|V|+|E|))`).** הרץ [[Breadth-First Search (BFS)|חיפוש לרוחב (BFS)]] או [[Depth-First Search (DFS)|חיפוש לעומק (DFS)]] פעם אחת מ**כל** צומת; כל מה שמתגלה מ-`u` הוא בדיוק השורה של `u` ב-`G*`. מהיר מ-`O(|V|³)` בגרפים **דלילים**.

## פסאודו-קוד

```text
TRANSITIVE-CLOSURE(G):          # Floyd-Warshall בוליאני
    for all i, j:
        t[i][j] ← (i = j) OR ((i, j) ∈ E)
    for k ← 1 to n:
        for i ← 1 to n:
            for j ← 1 to n:
                t[i][j] ← t[i][j] OR (t[i][k] AND t[k][j])
    return t
```

## דוגמה

גרף מסלול `1 → 2 → 3`. הסגור שלו מוסיף את קשת הנגישות `1 → 3`:

| נגיש? | 1 | 2 | 3 |
|---|---|---|---|
| **1** | – | ✓ | ✓ |
| **2** | – | – | ✓ |
| **3** | – | – | – |

כלומר `E = {(1,2), (2,3)}` אבל `E* = {(1,2), (2,3), (1,3)}` — הקשת הנוספת `(1,3)` היא המסלול החדש שהיה מובלע ב-`G`.

> [!note] מדוע `OR/AND` במקום `min/+`?
> פלויד-וורשל שואל "האם יש מסלול **קצר יותר** דרך `k`?" הסגור הטרנזיטיבי שואל את הגרסה הבוליאנית: "האם יש מסלול **כלשהו** דרך `k`?" החלפת החיבור ב-AND (שני הקטעים חייבים להתקיים) והמינימום ב-OR (כל מסלול מספיק) הופכת את אותה לולאה משולשת לפותר נגישות.

## איך זה מופיע במבחן

הסגור הטרנזיטיבי כמעט תמיד מופיע כסעיף **רב-ברירה 3ה** (מ-2024 ואילך), או כשאלת תכנון `O(|V|³)`. התכונות החוזרות הן הלב של הנושא.

- **תכונות `G*` (רב-ברירה).** *(2025 סמ"ב מועד א, שאלה 3ה)* — היחס בין `G` ל-`G*`: כל קשת ב-`G` נמצאת ב-`G*`, הסגור של DAG נשאר חסר-מעגלים, וכו'. *(2024 סמ"ב מועד מיוחד / קיץ מועד ב / קיץ מועד ג, שאלה 3ה)* — טענות כגון "`|E*| ≥ |E|`" ו-"קיים סגור לכל גרף מכוון".
- **השוואת נגישות / הוספת קשתות.** *(2022 סמ"ב מועד א, שאלה 1ב — לא נכון)* — סגורים טרנזיטיביים שווים **אינם** גוררים `G₁ = G₂`. *(2024 סמ"ב מועד א, שאלה 3ה — רב-ברירה)* — איזו קשת יחידה להוסיף ל-`G₂` כך ש-`G₂* = G₁*`. *(2025 קיץ מועד א, שאלה 1א)* — מעגל מכוון על יותר מ-3 צמתים ⟹ `G*` מכיל מעגל **נוסף** שלא היה ב-`G`.
- **שאלת תכנון.** *(2024 סמ"ב מועד ב, שאלה 2ב)* — בדיקה ב-`O(|V|³)` אם גרף מכוון הוא "חד-צדדי" (לכל `u, v` יש מסלול `u → v` או `v → u`): חשב את הסגור עם Floyd-Warshall בוליאני, ואז בדוק שלכל זוג מתקיים כיוון אחד.

> [!info] העובדה השימושית
> **Floyd-Warshall בוליאני מחשב את הסגור הטרנזיטיבי ב-`O(|V|³)`** — זהו פשוטו כמשמעו [[Floyd–Warshall Algorithm|פלויד-וורשל]] עם `min → OR` ו-`+ → AND`. זכור שני אינווריאנטים שעליהם נשענים סעיפי הרב-ברירה: **כל קשת של `G` שורדת ב-`G*`** (`E ⊆ E*`), ו**הסגור של DAG נשאר חסר-מעגלים**. בגרפים דלילים, `|V|` ריצות BFS/DFS מנצחות את החסם הקובי.
